server.port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/xiaoqi?user=root&password=root&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username:
    password:
